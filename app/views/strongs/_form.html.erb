<%= form_for(@strong) do |f| %>
  <% if @strong.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@strong.errors.count, "error") %> prohibited this strong from being saved:</h2>

      <ul>
      <% @strong.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :phone %><br />
    <%= f.number_field :phone %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
